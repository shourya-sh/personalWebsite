# Dependencies
node_modules/

# Local env and secrets (Vite exposes only VITE_* to the client; still keep private)
.env
.env.*
!.env.example

# Logs and scratch output
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
build.log
output.txt
output_horizontal.txt

# Vite / TypeScript caches
.vite/
*.tsbuildinfo

# Test coverage (if you add tests later)
coverage/

# OS junk
.DS_Store
Thumbs.db

# Editors
.idea/
*.swp
*~

# dist/ is intentionally not ignored — build output is committed for static hosting.
# If you switch to CI deploys, add: /dist
